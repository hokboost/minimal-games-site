<!DOCTYPE html>
<html lang="<%= lang === 'zh' ? 'zh-CN' : 'en' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang === 'zh' ? '游戏专区' : 'Game Zone' %></title>
    <link rel="stylesheet" href="/styles.css">
    <style>
    .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid rgba(255, 255, 255, 0.2);
    }
    
    .title {
        margin: 0;
        color: #fff;
    }
    
    .user-info {
        display: flex;
        align-items: center;
        gap: 1rem;
        color: #ccc;
    }
    
    .user-menu {
        display: flex;
        gap: 0.5rem;
    }
    
    .auth-menu {
        display: flex;
        gap: 0.5rem;
    }
    
    .btn-profile, .btn-admin, .btn-logout, .btn-login, .btn-register {
        padding: 0.5rem 1rem;
        border-radius: 8px;
        text-decoration: none;
        font-size: 0.9rem;
        font-weight: bold;
        transition: all 0.3s ease;
        border: none;
        cursor: pointer;
    }
    
    .btn-profile {
        background: linear-gradient(135deg, #2196f3, #1976d2);
        color: white;
    }
    
    .btn-profile:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(33, 150, 243, 0.4);
    }
    
    .btn-admin {
        background: linear-gradient(135deg, #ff9800, #f57c00);
        color: white;
    }
    
    .btn-admin:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(255, 152, 0, 0.4);
    }
    
    .btn-logout {
        background: linear-gradient(135deg, #f44336, #d32f2f);
        color: white;
    }
    
    .btn-logout:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(244, 67, 54, 0.4);
    }
    
    .btn-login {
        background: linear-gradient(135deg, #4caf50, #45a049);
        color: white;
    }
    
    .btn-login:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(76, 175, 80, 0.4);
    }
    
    .btn-register {
        background: linear-gradient(135deg, #00c853, #00bfa5);
        color: white;
    }
    
    .btn-register:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 200, 83, 0.4);
    }
    
    @media (max-width: 768px) {
        .header {
            flex-direction: column;
            gap: 1rem;
        }
        
        .user-menu, .auth-menu {
            flex-wrap: wrap;
        }
    }
    
    .games-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
    }
    
    .game-card {
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 12px;
        padding: 2rem;
        text-align: center;
        transition: all 0.3s;
        cursor: pointer;
        text-decoration: none;
        color: inherit;
        display: block;
    }
    
    .game-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        background: rgba(255, 255, 255, 0.1);
        border-color: #f0c040;
    }
    
    .game-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        display: block;
    }
    
    .game-title {
        color: #f0c040;
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
        font-weight: bold;
    }
    
    .game-description {
        color: #ccc;
        font-size: 1rem;
        line-height: 1.5;
        margin-bottom: 1rem;
    }
    
    .game-status {
        margin-top: 1rem;
        padding: 0.5rem 1rem;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 20px;
        font-size: 0.9rem;
        display: inline-block;
    }
    
    .status-hot {
        color: #ff6b6b;
        border: 1px solid #ff6b6b;
    }
    
    .status-classic {
        color: #f0c040;
        border: 1px solid #f0c040;
    }
    
    @media (max-width: 768px) {
        .games-grid {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }
        
        .game-icon {
            font-size: 3rem;
        }
    }
    </style>
</head>
<body data-authorized="<%= user && user.authorized ? 'true' : 'false' %>" data-username="<%= user ? user.username : '' %>">
    <%- include('partials/language-switcher') %>

    <div class="container">

        <div class="header">
            <h1 class="title">🧭 <%= lang === 'zh' ? '游戏专区' : 'Game Zone' %></h1>
            <div class="user-info">
                <% if (typeof req !== 'undefined' && req.session && req.session.user) { %>
                    <span><%= lang === 'zh' ? '欢迎' : 'Welcome' %>，<%= req.session.user.username %>!</span>
                    <div class="user-menu">
                        <a href="/" class="btn-profile">🏠 <%= lang === 'zh' ? '返回首页' : 'Home' %></a>
                        <a href="/profile" class="btn-profile">👤 <%= lang === 'zh' ? '个人资料' : 'Profile' %></a>
                        <% if (req.session.user.is_admin) { %>
                            <a href="/admin" class="btn-admin">👑 <%= lang === 'zh' ? '管理后台' : 'Admin' %></a>
                        <% } %>
                        <a href="/logout" class="btn-logout">🚪 <%= lang === 'zh' ? '退出登录' : 'Logout' %></a>
                    </div>
                <% } else { %>
                    <div class="auth-menu">
                        <a href="/login" class="btn-login">🔐 <%= t.auth.login %></a>
                        <a href="/register" class="btn-register">🆕 <%= t.auth.register %></a>
                    </div>
                <% } %>
            </div>
        </div>
        
        <% if (typeof user !== 'undefined' && user && user.authorized) { %>
            <div class="spins-reminder">
                <h3>⚡ <%= lang === 'zh' ? '电币余额' : 'Coin Balance' %>：<%= balance !== null ? balance : 0 %> <%= lang === 'zh' ? '电币' : 'coins' %></h3>
                <p><%= lang === 'zh' ? '仅供娱乐，虚拟电币不可兑换真实货币' : 'For entertainment only, virtual coins cannot be exchanged for real money' %></p>
            </div>

            <div class="games-grid">
                <a href="/wish" class="game-card">
                    <span class="game-icon">🌟</span>
                    <h2 class="game-title"><%= lang === 'zh' ? '幸运祈愿' : 'Lucky Wish' %></h2>
                    <p class="game-description"><%= lang === 'zh' ? '许下心愿，看看幸运女神是否眷顾你' : 'Make a wish and see if Lady Luck favors you' %></p>
                    <span class="game-status status-classic">⭐ <%= lang === 'zh' ? '经典' : 'Classic' %></span>
                </a>

                <a href="/blindbox" class="game-card">
                    <span class="game-icon">🎁</span>
                    <h2 class="game-title"><%= lang === 'zh' ? '惊喜盲盒' : 'Surprise Blind Box' %></h2>
                    <p class="game-description"><%= lang === 'zh' ? '选择档位，惊喜礼物即刻送出' : 'Pick a tier and send surprise gifts instantly' %></p>
                    <span class="game-status status-hot">🎉 <%= lang === 'zh' ? '新上架' : 'New' %></span>
                </a>

                <a href="/stone" class="game-card">
                    <span class="game-icon">🪨</span>
                    <h2 class="game-title"><%= lang === 'zh' ? '合石头' : 'Stone Match' %></h2>
                    <p class="game-description"><%= lang === 'zh' ? '放入彩色石头，拼出更高奖励' : 'Place colored stones to earn bigger rewards' %></p>
                    <span class="game-status status-hot">🧩 <%= lang === 'zh' ? '新玩法' : 'New' %></span>
                </a>

                <a href="/flip" class="game-card">
                    <span class="game-icon">🃏</span>
                    <h2 class="game-title"><%= lang === 'zh' ? '翻卡牌' : 'Card Flip' %></h2>
                    <p class="game-description"><%= lang === 'zh' ? '翻开好牌，随时退出拿奖励' : 'Flip good cards, cash out anytime for rewards' %></p>
                    <span class="game-status status-hot">✨ <%= lang === 'zh' ? '刺激' : 'Exciting' %></span>
                </a>

                <a href="/duel" class="game-card">
                    <span class="game-icon">⚔️</span>
                    <h2 class="game-title"><%= lang === 'zh' ? '决斗挑战' : 'Duel Challenge' %></h2>
                    <p class="game-description"><%= lang === 'zh' ? '选择功力，挑战高额奖品' : 'Choose your power, challenge for big prizes' %></p>
                    <span class="game-status status-hot">🔥 <%= lang === 'zh' ? '热血' : 'Intense' %></span>
                </a>
            </div>
        <% } else { %>
            <div style="
                text-align: center;
                padding: 4rem 2rem;
                margin: 2rem 0;
                background: rgba(255, 255, 255, 0.05);
                border-radius: 12px;
                border: 1px solid rgba(255, 255, 255, 0.1);
            ">
                <% if (typeof user !== 'undefined' && user && !user.authorized) { %>
                    <div style="font-size: 3.6rem; margin-bottom: 1.5rem;">🔒</div>
                    <h2 style="color: #ff9800; margin-bottom: 1rem;">
                        <%= lang === 'zh' ? '账户待授权' : 'Account Pending Authorization' %>
                    </h2>
                    <p style="color: #ccc; font-size: 1.1rem; line-height: 1.6;">
                        <%= lang === 'zh'
                            ? '你的账号需要管理员授权后才能进入游戏。请联系管理员或稍后再试。'
                            : 'Your account needs admin approval before you can access the games. Please contact the admin or try again later.'
                        %>
                    </p>
                    <div style="margin-top: 1.5rem;">
                        <a href="/logout" class="btn-logout" style="display: inline-block;"><%= lang === 'zh' ? '退出登录' : 'Logout' %></a>
                    </div>
                <% } else { %>
                    <div style="font-size: 3.6rem; margin-bottom: 1.5rem;">👋</div>
                    <h2 style="color: #ff9800; margin-bottom: 1rem;">
                        <%= lang === 'zh' ? '请先登录或注册' : 'Please Login or Register' %>
                    </h2>
                    <p style="color: #ccc; font-size: 1.1rem; line-height: 1.6;">
                        <%= lang === 'zh'
                            ? '登录后即可进入游戏专区。'
                            : 'Sign in to enter the game zone.'
                        %>
                    </p>
                    <div style="margin-top: 1.5rem; display: inline-flex; gap: 0.8rem; flex-wrap: wrap; justify-content: center;">
                        <a href="/login" class="btn-login">🔐 <%= t.auth.login %></a>
                        <a href="/register" class="btn-register">🆕 <%= t.auth.register %></a>
                    </div>
                <% } %>
            </div>
        <% } %>
    </div>

<% if (typeof user !== 'undefined' && user && user.authorized) { %>
<script src="/socket.io/socket.io.js"></script>
<script src="/security-alerts.js"></script>
<script src="/js/index.js"></script>
<% } %>

</body>
</html>
