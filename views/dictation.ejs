<!DOCTYPE html>
<html lang="<%= lang === 'zh' ? 'zh-CN' : 'en' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang === 'zh' ? '汉字听写' : 'Chinese Dictation' %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body data-username="<%= username %>" data-csrf-token="<%= csrfToken %>">
    <%- include('partials/language-switcher') %>

    <div class="container">
        <div class="header">
            <h1>✍️ <%= lang === 'zh' ? '汉字听写' : 'Chinese Dictation' %></h1>
            <div class="nav-links">
                <span><%= lang === 'zh' ? '用户' : 'User' %>：<%= username %></span>
                <a href="/">🏠 <%= lang === 'zh' ? '返回首页' : 'Home' %></a>
                <a href="/profile">👤 <%= lang === 'zh' ? '个人中心' : 'Profile' %></a>
            </div>
        </div>

        <div class="dictation-panel">
            <div class="dictation-progress">
                <span id="dictation-progress"><%= lang === 'zh' ? '未开始' : 'Not started' %></span>
                <span class="dictation-subtext"><%= lang === 'zh' ? '点击开始后自动朗读' : 'Auto read after you start' %></span>
            </div>

            <div class="dictation-controls">
                <button id="start-btn" class="primary-btn">▶️ <%= lang === 'zh' ? '开始听写' : 'Start' %></button>
            </div>

            <div class="dictation-audio">
                <span class="dictation-audio-label"><%= lang === 'zh' ? '题目播报' : 'Prompt' %></span>
                <button id="play-btn" class="speaker-btn" disabled title="<%= lang === 'zh' ? '播放词语与解释' : 'Play word and meaning' %>">🔊</button>
            </div>

            <div class="input-section">
                <label><%= lang === 'zh' ? '同音字选择' : 'Homophone Selection' %></label>
                <div class="dictation-pinyin" id="dictation-pinyin"></div>
                <div class="dictation-choice" id="dictation-choice">
                    <div class="dictation-phrase" id="dictation-phrase"></div>
                    <div class="dictation-homophone-panel">
                        <div class="dictation-homophone-title" id="dictation-homophone-title"></div>
                        <div class="dictation-homophone-list" id="dictation-homophone-list"></div>
                        <div class="dictation-choice-actions">
                            <button id="clear-choice-btn" class="ghost-btn" type="button"><%= lang === 'zh' ? '清空选择' : 'Clear selection' %></button>
                        </div>
                    </div>
                </div>
                <div class="dictation-definition" id="dictation-definition"></div>
            </div>

            <div class="submit-controls">
                <button id="submit-btn" class="primary-btn" disabled>✅ <%= lang === 'zh' ? '确认提交' : 'Submit' %></button>
            </div>

            <div id="dictation-status" class="dictation-status"></div>
        </div>

        <div class="dictation-note">
            <% if (lang === 'zh') { %>
                <strong>使用说明：</strong>点击开始后系统会自动播报词语与解释，请按拼音逐字选择同音字完成词语并提交。提交后进入人工审核，结果以后台人工判定为准。
            <% } else { %>
                <strong>How it works:</strong> After you start, the page reads the word and meaning aloud. Pick a homophone for each syllable and submit. Submissions are manually reviewed on the backend.
            <% } %>
        </div>
    </div>

    <div id="dictation-confirm" class="dictation-confirm" hidden>
        <div class="dictation-confirm-card">
            <h3><%= lang === 'zh' ? '确认开始听写？' : 'Start dictation?' %></h3>
            <p><%= lang === 'zh' ? '确认后会扣除一次听写次数。' : 'Confirming will use one attempt.' %></p>
            <div class="dictation-confirm-actions">
                <button id="confirm-start-btn" class="primary-btn"><%= lang === 'zh' ? '确认开始' : 'Confirm' %></button>
                <button id="cancel-start-btn" class="ghost-btn"><%= lang === 'zh' ? '取消' : 'Cancel' %></button>
            </div>
        </div>
    </div>

    <script src="/js/dictation.js"></script>

    <style>
        .container {
            max-width: 960px;
            margin: 0 auto;
            padding: 2rem;
            background: rgba(0, 0, 0, 0.8);
            border-radius: 16px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.45);
            color: #fff;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #4dd0e1;
        }

        .header h1 {
            margin: 0;
            color: #4dd0e1;
        }

        .nav-links {
            display: flex;
            gap: 1rem;
            align-items: center;
            color: #ccc;
        }

        .nav-links a {
            color: #fff;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }

        .nav-links a:hover {
            background: rgba(77, 208, 225, 0.2);
            transform: translateY(-2px);
        }

        .dictation-panel {
            background: rgba(255, 255, 255, 0.06);
            border: 1px solid rgba(255, 255, 255, 0.12);
            border-radius: 16px;
            padding: 2rem;
        }

        .dictation-progress {
            display: flex;
            flex-direction: column;
            gap: 0.3rem;
            font-size: 1.1rem;
            color: #f6e58d;
            margin-bottom: 1.5rem;
        }

        .dictation-subtext {
            font-size: 0.9rem;
            color: #b2bec3;
        }


        .dictation-controls,
        .submit-controls {
            display: flex;
            flex-wrap: wrap;
            gap: 0.8rem;
            margin-bottom: 1.5rem;
        }

        .dictation-audio {
            display: flex;
            align-items: center;
            gap: 0.6rem;
            margin-bottom: 1.2rem;
        }

        .dictation-audio-label {
            color: #e0f7fa;
            font-weight: 600;
        }

        .speaker-btn {
            width: 42px;
            height: 42px;
            border-radius: 50%;
            border: none;
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            cursor: pointer;
            font-size: 1.1rem;
            transition: transform 0.2s ease, background 0.2s ease;
        }

        .speaker-btn:hover:not(:disabled) {
            background: rgba(77, 208, 225, 0.2);
            transform: translateY(-2px);
        }

        .primary-btn,
        .ghost-btn {
            border: none;
            border-radius: 999px;
            padding: 0.65rem 1.6rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .primary-btn {
            background: linear-gradient(135deg, #4dd0e1, #1de9b6);
            color: #04131a;
            box-shadow: 0 6px 18px rgba(29, 233, 182, 0.35);
        }

        .primary-btn:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(29, 233, 182, 0.45);
        }

        .ghost-btn {
            background: rgba(255, 255, 255, 0.08);
            color: #fff;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .ghost-btn:hover:not(:disabled) {
            background: rgba(77, 208, 225, 0.2);
        }

        button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            box-shadow: none;
        }

        .input-section {
            display: flex;
            flex-direction: column;
            gap: 0.6rem;
            margin-bottom: 1.5rem;
        }

        .input-section label {
            color: #e0f7fa;
            font-weight: 600;
        }

        .dictation-grid {
            display: grid;
            grid-template-columns: repeat(4, minmax(60px, 1fr));
            gap: 0.8rem;
            max-width: 360px;
        }

        .dictation-pinyin {
            display: flex;
            flex-wrap: wrap;
            gap: 0.6rem;
            margin-bottom: 0.6rem;
            color: #4dd0e1;
            font-size: 0.85rem;
            letter-spacing: 0.5px;
        }

        .dictation-pinyin-cell {
            min-width: 3.2rem;
            text-align: center;
            padding: 0.2rem 0.4rem;
            background: rgba(77, 208, 225, 0.08);
            border-radius: 6px;
        }

        .dictation-pinyin-cell.disabled {
            opacity: 0.5;
        }

        .dictation-choice {
            display: grid;
            gap: 0.9rem;
            align-items: start;
            max-width: 640px;
        }

        .dictation-phrase {
            display: flex;
            flex-wrap: wrap;
            gap: 0.6rem;
        }

        .dictation-phrase-cell {
            width: 64px;
            height: 64px;
            border-radius: 12px;
            border: 2px solid rgba(77, 208, 225, 0.35);
            background: rgba(0, 0, 0, 0.35);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.7rem;
            color: #fff;
            cursor: pointer;
            transition: border-color 0.2s ease, transform 0.2s ease;
        }

        .dictation-phrase-cell.disabled {
            border-color: rgba(255, 255, 255, 0.15);
            color: rgba(255, 255, 255, 0.4);
            cursor: default;
        }

        .dictation-phrase-cell.active {
            border-color: rgba(77, 208, 225, 0.9);
            transform: translateY(-2px);
        }

        .dictation-homophone-panel {
            padding: 0.8rem 0.9rem;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.12);
        }

        .dictation-homophone-title {
            font-size: 0.95rem;
            color: #f6e58d;
            margin-bottom: 0.6rem;
        }

        .dictation-homophone-list {
            display: flex;
            flex-wrap: wrap;
            gap: 0.4rem;
            max-height: 240px;
            overflow-y: auto;
            padding-right: 0.2rem;
        }

        .dictation-homophone-btn {
            min-width: 36px;
            padding: 0.35rem 0.5rem;
            border-radius: 8px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            background: rgba(255, 255, 255, 0.08);
            color: #fff;
            font-size: 1rem;
            cursor: pointer;
            transition: transform 0.2s ease, border-color 0.2s ease, background 0.2s ease;
        }

        .dictation-homophone-btn:hover {
            border-color: rgba(77, 208, 225, 0.8);
            background: rgba(77, 208, 225, 0.2);
            transform: translateY(-1px);
        }

        .dictation-choice-actions {
            margin-top: 0.7rem;
            display: flex;
            justify-content: flex-end;
        }

        .dictation-choice-disabled {
            pointer-events: none;
            opacity: 0.6;
        }

        .dictation-homophone-empty {
            color: #b2bec3;
            font-size: 0.9rem;
        }

        .dictation-cell {
            width: 100%;
            aspect-ratio: 1 / 1;
            background:
                linear-gradient(to right, transparent 48%, rgba(255, 255, 255, 0.3) 49%, rgba(255, 255, 255, 0.3) 51%, transparent 52%),
                linear-gradient(to bottom, transparent 48%, rgba(255, 255, 255, 0.3) 49%, rgba(255, 255, 255, 0.3) 51%, transparent 52%),
                rgba(0, 0, 0, 0.45);
            border: 1px solid rgba(77, 208, 225, 0.4);
            border-radius: 10px;
            touch-action: none;
        }

        .dictation-grid-disabled .dictation-cell {
            pointer-events: none;
            opacity: 0.7;
        }

        .dictation-grid-actions {
            display: flex;
            align-items: center;
            gap: 0.8rem;
            margin-top: 0.6rem;
        }

        .dictation-grid-hint {
            color: #b2bec3;
            font-size: 0.85rem;
        }

        .dictation-definition {
            margin-top: 0.8rem;
            padding: 0.7rem 0.9rem;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.12);
            border-radius: 10px;
            color: #e0f7fa;
            font-size: 0.9rem;
            line-height: 1.6;
            max-width: 520px;
        }

        .ghost-btn.active {
            background: rgba(77, 208, 225, 0.35);
            border-color: rgba(77, 208, 225, 0.8);
        }

        .dictation-status {
            min-height: 1.6rem;
            color: #ffeb3b;
            font-size: 0.95rem;
        }

        .dictation-note {
            margin-top: 2rem;
            padding: 1rem 1.2rem;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.12);
            border-radius: 12px;
            color: #ccc;
            line-height: 1.6;
        }

        .dictation-confirm {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.6);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
            z-index: 10000;
        }

        .dictation-confirm-card {
            width: 100%;
            max-width: 420px;
            background: #0d1b21;
            border: 1px solid rgba(77, 208, 225, 0.4);
            border-radius: 16px;
            padding: 1.6rem;
            text-align: center;
            color: #fff;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.45);
        }

        .dictation-confirm-card h3 {
            margin: 0 0 0.6rem;
            color: #4dd0e1;
        }

        .dictation-confirm-card p {
            margin: 0 0 1.4rem;
            color: #cfd8dc;
        }

        .dictation-confirm-actions {
            display: flex;
            justify-content: center;
            gap: 0.8rem;
            flex-wrap: wrap;
        }

        .dictation-zoom {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.7);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
            z-index: 10001;
        }

        .dictation-zoom-card {
            width: min(520px, 92vw);
            background: #0b151b;
            border: 1px solid rgba(77, 208, 225, 0.4);
            border-radius: 18px;
            padding: 1rem;
            display: flex;
            flex-direction: column;
            gap: 0.8rem;
        }

        .dictation-zoom-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            color: #e0f7fa;
            font-weight: 600;
        }

        #dictation-zoom-canvas {
            width: 100%;
            aspect-ratio: 1 / 1;
            border-radius: 12px;
            background:
                linear-gradient(to right, transparent 48%, rgba(255, 255, 255, 0.2) 49%, rgba(255, 255, 255, 0.2) 51%, transparent 52%),
                linear-gradient(to bottom, transparent 48%, rgba(255, 255, 255, 0.2) 49%, rgba(255, 255, 255, 0.2) 51%, transparent 52%),
                rgba(0, 0, 0, 0.55);
        }

        .dictation-zoom-actions {
            display: flex;
            gap: 0.6rem;
            justify-content: flex-end;
            flex-wrap: wrap;
        }

        @media (max-width: 768px) {
            .container {
                padding: 1.5rem;
            }

            .header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }

            .nav-links {
                flex-wrap: wrap;
            }

            .dictation-grid {
                max-width: 100%;
                grid-template-columns: repeat(4, minmax(54px, 1fr));
            }

            .dictation-cell {
                font-size: 1.4rem;
            }
        }
    </style>
</body>
</html>
