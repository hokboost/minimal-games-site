<!DOCTYPE html>
<html lang="<%= lang === 'zh' ? 'zh-CN' : 'en' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang === 'zh' ? '请登录或注册' : 'Please Login or Register' %></title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .container {
            max-width: 720px;
            margin: 0 auto;
            padding: 2.5rem 1.5rem;
            text-align: center;
        }

        .status-card {
            background: rgba(0, 0, 0, 0.75);
            border-radius: 16px;
            padding: 2.5rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
            color: #fff;
        }

        .status-card h1 {
            margin: 0 0 1rem;
            color: #ffeb3b;
        }

        .status-card p {
            color: #ddd;
            margin-bottom: 2rem;
        }

        .action-btn {
            display: inline-block;
            padding: 0.8rem 2rem;
            border-radius: 999px;
            background: linear-gradient(45deg, #ff7043, #ff1744);
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            margin: 0 0.35rem;
        }
        .action-btn.secondary {
            background: linear-gradient(45deg, #00c853, #00bfa5);
        }
    </style>
</head>
<body class="app-redesign page-coming-soon">
    <%- include('partials/language-switcher') %>

    <div class="container">
        <div class="status-card">
            <h1><%= lang === 'zh' ? '请先登录或注册' : 'Please Login or Register' %></h1>
            <p>
                <%= lang === 'zh'
                    ? '登录后即可访问全部游戏功能与礼物系统。'
                    : 'Log in to access all games and the gift system.'
                %>
            </p>
            <% if (typeof user !== 'undefined' && user) { %>
                <a class="action-btn" href="/logout">
                    <%= lang === 'zh' ? '退出登录' : 'Logout' %>
                </a>
            <% } else { %>
                <a class="action-btn secondary" href="/login">
                    <%= lang === 'zh' ? '登录' : 'Login' %>
                </a>
                <a class="action-btn" href="/register">
                    <%= lang === 'zh' ? '注册' : 'Register' %>
                </a>
            <% } %>
        </div>
    </div>
</body>
</html>
