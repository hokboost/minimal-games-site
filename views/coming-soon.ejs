<!DOCTYPE html>
<html lang="<%= lang === 'zh' ? 'zh-CN' : 'en' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang === 'zh' ? '开发中' : 'Under Development' %></title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .container {
            max-width: 720px;
            margin: 0 auto;
            padding: 2.5rem 1.5rem;
            text-align: center;
        }

        .status-card {
            background: rgba(0, 0, 0, 0.75);
            border-radius: 16px;
            padding: 2.5rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
            color: #fff;
        }

        .status-card h1 {
            margin: 0 0 1rem;
            color: #ffeb3b;
        }

        .status-card p {
            color: #ddd;
            margin-bottom: 2rem;
        }

        .logout-btn {
            display: inline-block;
            padding: 0.8rem 2rem;
            border-radius: 999px;
            background: linear-gradient(45deg, #ff7043, #ff1744);
            color: #fff;
            text-decoration: none;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <%- include('partials/language-switcher') %>

    <div class="container">
        <div class="status-card">
            <h1>🚧 <%= lang === 'zh' ? '网站开发中' : 'Website Under Development' %></h1>
            <p>
                <%= lang === 'zh'
                    ? '当前服务仍在准备中，敬请期待。'
                    : 'Current service is still under preparation. Stay tuned.'
                %>
            </p>
            <a class="logout-btn" href="/logout">
                <%= lang === 'zh' ? '退出登录' : 'Logout' %>
            </a>
        </div>
    </div>
</body>
</html>
