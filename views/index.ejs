<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>游戏中心</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
    .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid rgba(255, 255, 255, 0.2);
    }
    
    .title {
        margin: 0;
        color: #fff;
    }
    
    .user-info {
        display: flex;
        align-items: center;
        gap: 1rem;
        color: #ccc;
    }
    
    .user-menu {
        display: flex;
        gap: 0.5rem;
    }
    
    .auth-menu {
        display: flex;
        gap: 0.5rem;
    }
    
    .btn-profile, .btn-admin, .btn-logout, .btn-login, .btn-register {
        padding: 0.5rem 1rem;
        border-radius: 8px;
        text-decoration: none;
        font-size: 0.9rem;
        font-weight: bold;
        transition: all 0.3s ease;
        border: none;
        cursor: pointer;
    }
    
    .btn-profile {
        background: linear-gradient(135deg, #2196f3, #1976d2);
        color: white;
    }
    
    .btn-profile:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(33, 150, 243, 0.4);
    }
    
    .btn-admin {
        background: linear-gradient(135deg, #ff9800, #f57c00);
        color: white;
    }
    
    .btn-admin:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(255, 152, 0, 0.4);
    }
    
    .btn-logout {
        background: linear-gradient(135deg, #f44336, #d32f2f);
        color: white;
    }
    
    .btn-logout:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(244, 67, 54, 0.4);
    }
    
    .btn-login {
        background: linear-gradient(135deg, #4caf50, #45a049);
        color: white;
    }
    
    .btn-login:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(76, 175, 80, 0.4);
    }
    
    .btn-register {
        background: linear-gradient(135deg, #00c853, #00bfa5);
        color: white;
    }
    
    .btn-register:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 200, 83, 0.4);
    }
    
    @media (max-width: 768px) {
        .header {
            flex-direction: column;
            gap: 1rem;
        }
        
        .user-menu, .auth-menu {
            flex-wrap: wrap;
        }
    }
    
    .games-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
    }
    
    .game-card {
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 12px;
        padding: 2rem;
        text-align: center;
        transition: all 0.3s;
        cursor: pointer;
        text-decoration: none;
        color: inherit;
        display: block;
    }
    
    .game-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        background: rgba(255, 255, 255, 0.1);
        border-color: #f0c040;
    }
    
    .game-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        display: block;
    }
    
    .game-title {
        color: #f0c040;
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
        font-weight: bold;
    }
    
    .game-description {
        color: #ccc;
        font-size: 1rem;
        line-height: 1.5;
        margin-bottom: 1rem;
    }
    
    .game-status {
        margin-top: 1rem;
        padding: 0.5rem 1rem;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 20px;
        font-size: 0.9rem;
        display: inline-block;
    }
    
    .status-new {
        color: #4ecdc4;
        border: 1px solid #4ecdc4;
    }
    
    .status-hot {
        color: #ff6b6b;
        border: 1px solid #ff6b6b;
    }
    
    .status-classic {
        color: #f0c040;
        border: 1px solid #f0c040;
    }
    
    @media (max-width: 768px) {
        .games-grid {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }
        
        .game-icon {
            font-size: 3rem;
        }
    }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="title">🎮 Minimal Games 游戏中心</h1>
            <div class="user-info">
                <% if (typeof req !== 'undefined' && req.session && req.session.user) { %>
                    <span>欢迎，<%= req.session.user.username %>!</span>
                    <div class="user-menu">
                        <a href="/profile" class="btn-profile" onclick="console.log('个人资料按钮被点击'); return true;">👤 个人资料</a>
                        <% if (req.session.user.is_admin) { %>
                            <a href="/admin" class="btn-admin" onclick="console.log('管理后台按钮被点击'); return true;">👑 管理后台</a>
                        <% } %>
                        <a href="/logout" class="btn-logout">🚺 退出登录</a>
                    </div>
                <% } else { %>
                    <div class="auth-menu">
                        <a href="/login" class="btn-login">🔐 登录</a>
                        <a href="/register" class="btn-register">🆕 注册</a>
                    </div>
                <% } %>
            </div>
        </div>
        
        <!-- 任务转盘功能暂时隐藏
        <div class="trigger-rules">
            <h2>🎡 转盘触发规则</h2>
            <div class="rules-grid">
                <div class="rule-item">
                    <span class="rule-icon">⏰</span>
                    <div class="rule-text">
                        <strong>定时触发：</strong><br>
                        如果过去30分钟没有转盘，强制触发一次
                    </div>
                </div>
                <div class="rule-item">
                    <span class="rule-icon">🎁</span>
                    <div class="rule-text">
                        <strong>礼物累计：</strong><br>
                        礼物累计到500票触发一次转盘
                    </div>
                </div>
                <div class="rule-item">
                    <span class="rule-icon">⚓</span>
                    <div class="rule-text">
                        <strong>指定特权：</strong><br>
                        单人送礼500票可指定转盘任务
                    </div>
                </div>
            </div>
        </div>
        -->
        
        <% if (typeof user !== 'undefined' && user) { %>
            <div class="spins-reminder">
                <h3>⚡ 电币余额：<%= balance || 0 %> 电币</h3>
                <p>仅供娱乐，虚拟电币不可兑换真实货币</p>
            </div>
        <% } %>

        <div class="games-grid">
            <!-- 任务转盘游戏暂时隐藏
            <a href="javascript:void(0)" onclick="startSpinGame()" class="game-card">
                <span class="game-icon">🎡</span>
                <h2 class="game-title">幸运转盘</h2>
                <p class="game-description">转动幸运转盘，赢取丰厚奖励和游戏次数</p>
                <span class="game-status status-hot">🔥 热门</span>
            </a>
            -->
            
            <a href="/quiz" class="game-card">
                <span class="game-icon">🧠</span>
                <h2 class="game-title">知识问答</h2>
                <p class="game-description">测试你的知识，答对越多奖励越丰厚</p>
                <span class="game-status status-hot">🔥 热门</span>
            </a>
            
            <a href="javascript:void(0)" onclick="startSlotGame()" class="game-card">
                <span class="game-icon">🎰</span>
                <h2 class="game-title">幸运老虎机</h2>
                <p class="game-description">经典老虎机游戏，三个相同图案即可获胜</p>
                <span class="game-status status-classic">⭐ 经典</span>
            </a>
            
            <a href="javascript:void(0)" onclick="startScratchGame()" class="game-card">
                <span class="game-icon">🎟️</span>
                <h2 class="game-title">刮刮乐</h2>
                <p class="game-description">刮开涂层，发现隐藏的奖励，简单又刺激</p>
                <span class="game-status status-classic">⭐ 经典</span>
            </a>
            
            <a href="javascript:void(0)" onclick="startWishGame()" class="game-card">
                <span class="game-icon">🌟</span>
                <h2 class="game-title">许愿星</h2>
                <p class="game-description">许下心愿，看看幸运女神是否眷顾你</p>
                <span class="game-status status-new">✨ 新游戏</span>
            </a>
        </div>
        
        <!-- 免责声明 -->
        <div style="
            max-width: 800px;
            margin: 3rem auto 0;
            padding: 1.5rem;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            font-size: 0.9rem;
            color: #ccc;
            text-align: center;
            line-height: 1.6;
        ">
            <div style="margin-bottom: 10px; font-weight: bold; color: #ff9800;">
                ⚠️ 重要声明
            </div>
            本平台所有游戏功能仅供娱乐消遣，不涉及任何形式的赌博、博彩或真实货币交易。<br>
            平台内虚拟积分、余额等均为娱乐道具，无法兑换为真实货币或有价物品。<br>
            参与完全免费，无需任何付费，符合中华人民共和国相关法律法规。<br>
            <strong>严禁未满18周岁用户使用，请理性娱乐，适度游戏。</strong>
        </div>
    </div>

<script>
// 游戏服务器预热和跳转函数

function createLoadingModal(gameName, iconEmoji, serverUrl) {
    const loadingModal = document.createElement('div');
    loadingModal.style.cssText = `
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.8);
        z-index: 9999;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        font-size: 1.5rem;
    `;
    loadingModal.innerHTML = `
        <div style="text-align: center; padding: 40px; background: rgba(255,255,255,0.1); border-radius: 15px; backdrop-filter: blur(10px);">
            <div style="font-size: 2rem; margin-bottom: 20px;">${iconEmoji}</div>
            <div>正在启动${gameName}...</div>
            <div style="margin-top: 15px; font-size: 1.1rem; color: #aaa;">Render免费版启动需要30-60秒，请耐心等待</div>
            <div style="margin-top: 20px;">
                <div class="loading-spinner"></div>
            </div>
        </div>
        <style>
            .loading-spinner {
                width: 30px;
                height: 30px;
                border: 3px solid rgba(255,255,255,0.3);
                border-top: 3px solid white;
                border-radius: 50%;
                animation: spin 1s linear infinite;
                margin: 0 auto;
            }
            @keyframes spin {
                0% { transform: rotate(0deg); }
                100% { transform: rotate(360deg); }
            }
        </style>
    `;
    document.body.appendChild(loadingModal);
    return loadingModal;
}

function startSlotGame() {
    const modal = createLoadingModal('幸运老虎机', '🎰', 'https://slot-server-9682.onrender.com');
    
    // 预热服务器
    fetch("https://slot-server-9682.onrender.com/ping")
        .then(() => {
            setTimeout(() => {
                window.location.href = "/slot";
            }, 3000); // 3秒后跳转
        })
        .catch(() => {
            // 即使ping失败也跳转，让老虎机页面处理
            setTimeout(() => {
                window.location.href = "/slot";
            }, 15000); // 15秒后跳转
        });
}

function startScratchGame() {
    const modal = createLoadingModal('刮刮乐', '🎟️', 'https://scratch-server-vmit.onrender.com');
    
    // 预热服务器
    fetch("https://scratch-server-vmit.onrender.com/ping")
        .then(() => {
            setTimeout(() => {
                window.location.href = "/scratch";
            }, 3000); // 3秒后跳转
        })
        .catch(() => {
            // 即使ping失败也跳转，让刮刮乐页面处理
            setTimeout(() => {
                window.location.href = "/scratch";
            }, 15000); // 15秒后跳转
        });
}

function startWishGame() {
    const modal = createLoadingModal('许愿星', '🌟', 'https://wish-server.onrender.com');
    
    // 预热服务器
    fetch("https://wish-server.onrender.com/ping")
        .then(() => {
            setTimeout(() => {
                window.location.href = "/wish";
            }, 3000); // 3秒后跳转
        })
        .catch(() => {
            // 即使ping失败也跳转，让许愿星页面处理
            setTimeout(() => {
                window.location.href = "/wish";
            }, 15000); // 15秒后跳转
        });
}

function startSpinGame() {
    const modal = createLoadingModal('幸运转盘', '🎡', 'https://secure-spin-server.onrender.com');
    
    // 预热服务器
    fetch("https://secure-spin-server.onrender.com/ping")
        .then(() => {
            setTimeout(() => {
                window.location.href = "/spin";
            }, 3000); // 3秒后跳转
        })
        .catch(() => {
            // 即使ping失败也跳转，让转盘页面处理
            setTimeout(() => {
                window.location.href = "/spin";
            }, 15000); // 15秒后跳转
        });
}
</script>

</body>
</html>