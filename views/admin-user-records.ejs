<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <div class="admin-header">
            <h1>🧾 用户记录：<%= targetUsername %></h1>
            <div class="nav-links">
                <a href="/admin">⬅️ 返回管理后台</a>
                <a href="/logout">🚪 退出登录</a>
            </div>
        </div>

        <div class="records-section">
            <h2>Quiz</h2>
            <table>
                <thead>
                    <tr>
                        <th>时间</th>
                        <th>分数</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (records.quiz.length === 0) { %>
                        <tr><td colspan="2">暂无记录</td></tr>
                    <% } else { %>
                        <% records.quiz.forEach(row => { %>
                            <tr>
                                <td><%= row.played_at %></td>
                                <td><%= row.score %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="records-section">
            <h2>Slot</h2>
            <table>
                <thead>
                    <tr>
                        <th>时间</th>
                        <th>结果</th>
                        <th>下注</th>
                        <th>倍数</th>
                        <th>派奖</th>
                        <th>面板</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (records.slot.length === 0) { %>
                        <tr><td colspan="6">暂无记录</td></tr>
                    <% } else { %>
                        <% records.slot.forEach(row => { %>
                            <tr>
                                <td><%= row.played_at %></td>
                                <td><%= row.result %></td>
                                <td><%= row.bet_amount %></td>
                                <td><%= row.multiplier %></td>
                                <td><%= row.payout %></td>
                                <td><%= row.amounts %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="records-section">
            <h2>Scratch</h2>
            <table>
                <thead>
                    <tr>
                        <th>时间</th>
                        <th>档位</th>
                        <th>匹配</th>
                        <th>奖励</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (records.scratch.length === 0) { %>
                        <tr><td colspan="4">暂无记录</td></tr>
                    <% } else { %>
                        <% records.scratch.forEach(row => { %>
                            <tr>
                                <td><%= row.played_at %></td>
                                <td><%= row.tier_cost %></td>
                                <td><%= row.matches_count %></td>
                                <td><%= row.reward %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="records-section">
            <h2>Wish</h2>
            <table>
                <thead>
                    <tr>
                        <th>时间</th>
                        <th>礼物</th>
                        <th>消耗</th>
                        <th>结果</th>
                        <th>奖励</th>
                        <th>次数</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (records.wish.length === 0) { %>
                        <tr><td colspan="6">暂无记录</td></tr>
                    <% } else { %>
                        <% records.wish.forEach(row => { %>
                            <tr>
                                <td><%= row.played_at %></td>
                                <td><%= row.gift_type || '-' %></td>
                                <td><%= row.cost %></td>
                                <td><%= row.success ? '成功' : '失败' %></td>
                                <td><%= row.reward || '-' %></td>
                                <td><%= row.wishes_count %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="records-section">
            <h2>Stone</h2>
            <table>
                <thead>
                    <tr>
                        <th>时间</th>
                        <th>动作</th>
                        <th>消耗</th>
                        <th>奖励</th>
                        <th>槽位</th>
                        <th>变化</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (records.stone.length === 0) { %>
                        <tr><td colspan="6">暂无记录</td></tr>
                    <% } else { %>
                        <% records.stone.forEach(row => { %>
                            <tr>
                                <td><%= row.played_at %></td>
                                <td><%= row.action_type %></td>
                                <td><%= row.cost %></td>
                                <td><%= row.reward %></td>
                                <td><%= row.slot_index !== null ? row.slot_index : '-' %></td>
                                <td><%= row.before_slots %> → <%= row.after_slots %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="records-section">
            <h2>Flip</h2>
            <table>
                <thead>
                    <tr>
                        <th>时间</th>
                        <th>动作</th>
                        <th>消耗</th>
                        <th>奖励</th>
                        <th>翻开</th>
                        <th>好/坏</th>
                        <th>结束</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (records.flip.length === 0) { %>
                        <tr><td colspan="7">暂无记录</td></tr>
                    <% } else { %>
                        <% records.flip.forEach(row => { %>
                            <tr>
                                <td><%= row.played_at %></td>
                                <td><%= row.action_type %></td>
                                <td><%= row.cost %></td>
                                <td><%= row.reward %></td>
                                <td><%= row.card_index !== null ? row.card_index : '-' %> / <%= row.card_type || '-' %></td>
                                <td><%= row.good_count %>/<%= row.bad_count %></td>
                                <td><%= row.ended ? '是' : '否' %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="records-section">
            <h2>Duel</h2>
            <table>
                <thead>
                    <tr>
                        <th>时间</th>
                        <th>礼物</th>
                        <th>功力</th>
                        <th>消耗</th>
                        <th>结果</th>
                        <th>奖励</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (records.duel.length === 0) { %>
                        <tr><td colspan="6">暂无记录</td></tr>
                    <% } else { %>
                        <% records.duel.forEach(row => { %>
                            <tr>
                                <td><%= row.played_at %></td>
                                <td><%= row.gift_type %></td>
                                <td><%= row.power %>%</td>
                                <td><%= row.cost %></td>
                                <td><%= row.success ? '成功' : '失败' %></td>
                                <td><%= row.reward %></td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <style>
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
            background: rgba(0, 0, 0, 0.85);
            border-radius: 15px;
            color: #fff;
        }

        .admin-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #ffb300;
        }

        .admin-header h1 {
            margin: 0;
            color: #ffeb3b;
        }

        .nav-links {
            display: flex;
            gap: 1rem;
        }

        .nav-links a {
            color: #fff;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.1);
        }

        .records-section {
            margin-bottom: 2rem;
        }

        .records-section h2 {
            color: #00c853;
            margin-bottom: 0.6rem;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 10px;
            overflow: hidden;
        }

        th, td {
            padding: 0.6rem 0.5rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
            text-align: left;
            font-size: 0.85rem;
            white-space: nowrap;
        }

        th {
            background: linear-gradient(135deg, #00c853, #00bfa5);
            color: #fff;
        }
    </style>
</body>
</html>
